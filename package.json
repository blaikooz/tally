{
  "name": "tally",
  "version": "1.0.0",
  "description": "Prediction tally tracker desktop app",
  "main": "main.js",
  "private": true,
  "scripts": {
    "start": "electron .",
    "package:mac": "electron-packager . Tally --platform=darwin --arch=x64 --icon=icon.icns --out=dist --overwrite"
  },
  "devDependencies": {
    "electron": "^32.0.0",
    "electron-packager": "^17.1.2"
  }
}
